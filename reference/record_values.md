# Validate and record responses into a standard object This is an internal function to generate wesp class object

Validate and record responses into a standard object This is an internal
function to generate wesp class object

## Usage

``` r
record_values(data, site)
```

## Arguments

- data:

  path to csv

- site:

  a number to show which site

## Value

a `list` object containing validated responses and question metadata
