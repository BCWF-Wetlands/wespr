# Retrieve all input responses from a `wesp_site` object as a data.frame.

Retrieve all input responses from a `wesp_site` object as a data.frame.

## Usage

``` r
get_responses(site, ...)
```

## Arguments

- site:

  A `wesp_site` object

- ...:

  ignored for now

## Value

A data.frame of responses
