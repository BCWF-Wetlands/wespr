# Check the input data for basic QA

Check the input data for basic QA

## Usage

``` r
check_indata(indata)
```

## Arguments

- indata:

  A path to the input data

## Value

messages to the console

## Examples

``` r
if (FALSE) { # \dontrun{
indata <- fs::path("inst/input_data/wetFlat_20250325.csv")
check_indata(indata)
} # }
```
