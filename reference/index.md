# Package index

## All functions

- [`as.wesp_site()`](as.wesp_site.md) :

  Convert a data.frame of WESP question responses to a `wesp_site`

- [`assign_jenks_score()`](assign_jenks_score.md) : Assign Jenks score
  based on calibration dataset

- [`build_report()`](build_report.md) : Build Site Report

- [`calc_indicators()`](calc_indicators.md) : Calculate Indicators for
  WESP assessment site

- [`calculate_jenks_score()`](calculate_jenks_score.md) : Calculate
  Jenks Breaks

- [`calculate_multi_site()`](calculate_multi_site.md) : Calculate
  indicators for multiple sites

- [`calibration_scores`](calibration_scores.md) : WESP calibrated site
  values

- [`check_indata()`](check_indata.md) : Check the input data for basic
  QA

- [`combine_rawdata()`](combine_rawdata.md) : Combine all types of raw
  data

- [`format_rawinputs()`](format_rawinputs.md) : Format raw inputs This
  function cleans, formats and combines raw outputs from field and
  desktop analysis survey123 forms.

- [`generate_ids()`](generate_ids.md) : Generate a key with names to be
  retained

- [`get_derived_values()`](get_derived_values.md) :

  Retrieve all derived values from a `wesp_site` object as a data.frame.

- [`get_indicator_scores()`](get_indicator_scores.md) :

  Retrieve indicator scores from a `wesp_site` object as a data.frame.

- [`get_responses()`](get_responses.md) :

  Retrieve all input responses from a `wesp_site` object as a
  data.frame.

- [`import_spatial_data()`](import_spatial_data.md) : Download base
  spatial information layers to support desktop analysis for Wespr

- [`load_wesp_data()`](load_wesp_data.md) : Load the data containing the
  responses to WESP form

- [`record_values()`](record_values.md) : Validate and record responses
  into a standard object This is an internal function to generate wesp
  class object

- [`wespr_workflow()`](wespr_workflow.md) : Run the entire wespr
  workflow
